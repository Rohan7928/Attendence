<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendence">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Change_password"></activity>
        <activity android:name=".Edit_Subject" />
        <activity android:name=".Update" />
        <activity android:name=".Take_attendence" />
        <activity android:name=".activity_cardview" />
        <activity android:name=".AddSubject" />
        <activity android:name=".activity_fragment" />
        <activity android:name=".activity_navigation" />
        <activity android:name=".activity_signup" />
        <activity android:name=".activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".activity_choose" />
        <activity android:name=".MainActivity">
        </activity>
    </application>

</manifest>